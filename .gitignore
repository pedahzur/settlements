.DS_Store
*.tmp
~$*
.ipynb_checkpoints/
__pycache__/
*.pyc
# Cached raw downloads from remote sources (regenerated by the pipeline).
db/raw/

# Large local GIS extracts are reference inputs, not pipeline dependencies.
/gis_osm_*

# Private manuscript drafts are maintained outside the public repository.
/writing/
